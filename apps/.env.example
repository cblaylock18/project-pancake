# App target to build (frontend or backend)
APP_TARGET=backend

# Database connection (Docker internal hostname)
DATABASE_URL=postgres://devuser:devpass@postgres:5432/devdb

# Optional: override ports
BACKEND_PORT=3000
POSTGRES_PORT=5432
